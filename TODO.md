# TODO

- [ ] Fix mobile menu toggle: update `script.js` to use the correct button id `mobile-menu-btn`.
- [ ] Fix duplicate theme toggle: remove one duplicate `id="theme-toggle"` button in `index.html`.
- [ ] (Optional) Add null-check guards in `script.js` for safer event listeners.
- [x] Test on mobile width (<768px>): hamburger opens/closes menu; clicking a link closes the menu.


